# History pagination — Path B
Add optional page/size parameters with SQL COUNT/LIMIT/OFFSET and stable newest-inserted order. Retain legacy array response when page is omitted. Separate Export/Import page controls with 20/50/100 size, total/range, previous/next, loading/error states. Clear resets both pagers. Verify records beyond old 200/500 limits, filters, invalid parameters, empty/clamped pages, and UI navigation.

Validation complete: 13 backend tests passed (221 Export / 521 Import records, SQL pagination, filtered totals, invalid inputs, empty/clamped pages, clear persistence, existing operations). Browser pager test passed with mocked history responses for independent navigation, 20/50/100 sizes, retry after failure, and mobile width. Existing two browser integration workflows passed with real isolated API/storage. Production build and sequential typecheck passed. Initial browser selector mismatch was corrected in the test; no product defect remained. Evidence under `_wrx-output/evidence/history-paging/`.
