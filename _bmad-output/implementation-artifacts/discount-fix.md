# Discount export and edit recalculation (Path B)
User overrides previous full-precision Excel policy: export each discount component with two decimals, HALF_UP; preserve internal source precision and disclose export net rounding delta. Keep template columns unchanged. Manual discount/price/quantity changes update effective gross/net; preserve first pre-edit values and raw source data, audit derived changes. Net edits reverse the rate. Source validation remains for untouched rows. Test single/bulk, returns, compound/boundary rates, exports, history, and source audit in isolated storage.
Red proof: `_wrx-output/evidence/discount-fix/red.txt` — 10 failing regressions before implementation.

Implemented: fixed-decimal text export, export net rounding delta header/history, immutable original-value snapshot, derived value audit, single/bulk/rule shared recalculation, non-persistent Decimal preview endpoint with debounced/cancelled UI requests. Source shop totals and costs remain evidence; calculated totals use edits. Re-saving an explicit discount repairs legacy stale settlement totals without clearing unrelated gross errors.
Validation: source audit passes 5 files / 3,896 rows / 1,008 SKU; export rounding delta +1.41 across grouped SKU (source/internal net unchanged 901536.74). Production build/typecheck and two browser workflows passed. See `_wrx-output/evidence/discount-fix/` for outputs. Local application was stopped; test servers use isolated ports/storage.
