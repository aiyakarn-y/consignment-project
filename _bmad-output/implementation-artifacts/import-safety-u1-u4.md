# U1–U4 implementation plan — Path C / codex-only
Scope follows the recommended first four steps: header drift validation, explicit workbook sheet selection, explicit settlement source for custom profiles, and Master replacement of fallback identifiers. Other review items remain separate backlog.
1. Bind mapped-column header text to profile via preview; reject missing/outdated header evidence on import, and ambiguous duplicate column assignments. Existing stored batches unchanged; old profiles require sample preview/save before next import.
2. Detect supported sheets, require explicit selection when multiple candidates, show selected/skipped sheet results. Allow same-layout profile across explicitly selected sheets, checking each header. Preserve built-in sample semantics and layout lookups.
3. Default profile net source to mapped net only; explicitly opt into cost-as-settlement. Missing net/discount uses editable 0%. Preserve actual source cost and GP/MG provenance.
4. Apply Master to empty/fallback SKU only, protect real/manual SKU, count and audit resolved replacements.
Evidence/red tests and source audit under _wrx-output/evidence/import-safety. Run focused tests per step, then full regression, typecheck/build and browser flows with isolated storage.

Implemented all four scoped steps. Added profile expected_headers/net_source and preview-bound config; workbook inventory and selection UI with selected/skipped sheet report; Master dry-run preview, fallback replacement audit, and unchanged-fallback provenance protection. No DB migration or reinterpretation of stored rows. Legacy profiles intentionally require preview/resave before new import. Different custom layouts across sheets require appropriate separate profile configuration; no silent inference.
Validation: red proof 7 failures before implementation, focused domain/API coverage expanded to 16 cases. Full regression 114 passed; source audit all 5 files/3,896 rows/1,008 SKU unchanged, expected two-decimal export delta +1.41. Build and typecheck passed. Browser evidence stored separately under _wrx-output/evidence/import-safety (initial test selectors/early file validation expectations corrected). No live data modified and no deployment.
Next backlog stages remain U5 emergency restore, U6 stale edit protection, U7 report revision/business duplicates, then period/batch navigation and operational readiness from the review. They are not claimed complete by this change.
Final browser verification: all four distinct workflows passed across the final relevant runs: history paging and operations/backup in e2e.txt, import safety in e2e-final.txt, and the real-files workflow in workflow-final.txt. The first failed browser checks were test selector/preflight mock assumptions; corrected tests now exercise actual early rejection plus explicit mocked mixed-result notification. Full suite final evidence: tests-final.txt (114 passed). Production build: build.txt; final typecheck: typecheck-final.txt. Existing on-disk sales and original reports were not changed.
